commit-msg:
  jobs:
    - run: bun run lint:commit --edit "{1}"

pre-commit:
  jobs:
    - run: bun run fix
      glob:
        - "**/*.js"
        - "**/*.jsx"
        - "**/*.ts"
        - "**/*.tsx"
        - "**/*.json"
        - "**/*.jsonc"
        - "**/*.css"
      stage_fixed: true

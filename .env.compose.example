# Local Docker Compose credentials only. Never use these in production.
POSTGRES_DB=mercel
POSTGRES_USER=mercel
POSTGRES_PASSWORD=mercel-local-secret
RUSTFS_ACCESS_KEY=mercel-local
RUSTFS_SECRET_KEY=mercel-local-secret
